#include <stdio.h>
/** main - introduction of the program
 *The program starts here
 *
 */
int main(void)
{
	char ch;
	for (ch = 'A'; ch<= 'Z'; ch++)
		putchar(ch);
             /* print lowercase letters */
	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
